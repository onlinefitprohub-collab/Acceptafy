FINAL PROMPT PART: Academy & Remaining Components
code
Markdown
Please continue creating the remaining files for the project. Use the exact code below.

### 1. `src/components/Academy/AcademyHub.tsx`
*(You provided this in the previous chunk, but ensure it imports all the modules below correctly. Re-pasting for safety.)*
```tsx
import React, { useState, useRef, useEffect } from 'react';
import { CloseIcon, AcademyIcon, ChecklistIcon, BodyIcon, CtaIcon, TeardownIcon, UserGroupIcon, AutomationIcon, AnalyticsIcon, LegalIcon, BranchingIcon, BlueprintIcon, MonitorIcon } from '../icons/CategoryIcons';
import { DeliverabilityFoundations } from './DeliverabilityFoundations';
import { ArtOfTheInbox } from './ArtOfTheInbox';
import { AdvancedStrategy } from './AdvancedStrategy';
import { RealWorldTeardowns } from './RealWorldTeardowns';
import { Glossary } from './Glossary';
import { ListSegmentation } from './ListSegmentation';
import { AutomationWorkflows } from './AutomationWorkflows';
import { AnalyticsKPIs } from './AnalyticsKPIs';
import { LegalCompliance } from './LegalCompliance';
import { ScenarioSimulator } from './ScenarioSimulator';
import { ModuleCard } from './ModuleComponents';
import { ColdOutreachBlueprint } from './ColdOutreachBlueprint';
import { NewsletterBlueprint } from './NewsletterBlueprint';
import { EcommerceBlueprint } from './EcommerceBlueprint';
import { SenderReputation } from './SenderReputation';
import { AdvancedAutomation } from './AdvancedAutomation';
import { ReEngagementBlueprint } from './ReEngagementBlueprint';

interface AcademyHubProps {
  onClose: () => void;
}

type ActiveTab = 'modules' | 'glossary';
type SelectedModule = 'foundations' | 'art' | 'strategy' | 'teardowns' | 'segmentation' | 'automation' | 'analytics' | 'legal' | 'simulator' | 'cold' | 'newsletter' | 'ecommerce' | 'senderReputation' | 'advancedAutomation' | 'reEngagement' | null;

const AcademyModuleSection: React.FC<{ title: string; subtitle: string; children: React.ReactNode }> = ({ title, subtitle, children }) => (
    <div>
        <h3 className="text-xl font-bold text-purple-300">{title}</h3>
        <p className="text-sm text-gray-400 mb-4">{subtitle}</p>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {children}
        </div>
    </div>
);

export const AcademyHub: React.FC<AcademyHubProps> = ({ onClose }) => {
    const [activeTab, setActiveTab] = useState<ActiveTab>('modules');
    const [selectedModule, setSelectedModule] = useState<SelectedModule>(null);
    const mainContentRef = useRef<HTMLDivElement>(null);

    useEffect(() => {
        if (mainContentRef.current) {
            mainContentRef.current.scrollTop = 0;
        }
    }, [selectedModule, activeTab]);

    const renderModuleContent = () => {
        switch (selectedModule) {
            case 'foundations': return <DeliverabilityFoundations onBack={() => setSelectedModule(null)} />;
            case 'art': return <ArtOfTheInbox onBack={() => setSelectedModule(null)} />;
            case 'strategy': return <AdvancedStrategy onBack={() => setSelectedModule(null)} />;
            case 'teardowns': return <RealWorldTeardowns onBack={() => setSelectedModule(null)} />;
            case 'segmentation': return <ListSegmentation onBack={() => setSelectedModule(null)} />;
            case 'automation': return <AutomationWorkflows onBack={() => setSelectedModule(null)} />;
            case 'analytics': return <AnalyticsKPIs onBack={() => setSelectedModule(null)} />;
            case 'legal': return <LegalCompliance onBack={() => setSelectedModule(null)} />;
            case 'simulator': return <ScenarioSimulator onBack={() => setSelectedModule(null)} />;
            case 'cold': return <ColdOutreachBlueprint onBack={() => setSelectedModule(null)} />;
            case 'newsletter': return <NewsletterBlueprint onBack={() => setSelectedModule(null)} />;
            case 'ecommerce': return <EcommerceBlueprint onBack={() => setSelectedModule(null)} />;
            case 'senderReputation': return <SenderReputation onBack={() => setSelectedModule(null)} />;
            case 'advancedAutomation': return <AdvancedAutomation onBack={() => setSelectedModule(null)} />;
            case 'reEngagement': return <ReEngagementBlueprint onBack={() => setSelectedModule(null)} />;
            default:
                return (
                    <div className="animate-fade-in space-y-8">
                         <h3 className="text-2xl font-bold text-white mb-2">Welcome to the Academy</h3>
                        <p className="text-gray-400 max-w-3xl">A curated learning path to help you master every aspect of email marketing. Start with the foundations, master the craft, and then learn the strategies that drive growth.</p>
                        
                        <AcademyModuleSection
                            title="Module 1: Maximize Your Deliverability"
                            subtitle="Build an unshakeable technical foundation to ensure your emails land in the inbox, not the spam folder."
                        >
                             <ModuleCard icon={<ChecklistIcon />} title="Unlock the Inbox: Master Authentication" description="Learn SPF, DKIM, & DMARC—the 'digital passports' that prove your emails are legit." onClick={() => setSelectedModule('foundations')} />
                             <ModuleCard icon={<LegalIcon />} title="Send with Confidence: Navigate Compliance" description="Master the rules of CAN-SPAM and GDPR to protect your business and your reputation." onClick={() => setSelectedModule('legal')} />
                             <ModuleCard icon={<UserGroupIcon />} title="Target Like a Pro: Master Your List" description="Discover the power of a clean, segmented list for radically better engagement." onClick={() => setSelectedModule('segmentation')} />
                            <ModuleCard icon={<MonitorIcon />} title="Sender Reputation Management" description="Learn how inbox providers 'score' you and how to stay in their good graces for maximum inbox placement." onClick={() => setSelectedModule('senderReputation')} />
                        </AcademyModuleSection>

                        <AcademyModuleSection
                            title="Module 2: Craft Emails That Convert"
                            subtitle="Learn the copywriting and psychological principles behind emails that get opened, read, and acted upon."
                        >
                            <ModuleCard icon={<BodyIcon />} title="Write Emails That Get Opened & Read" description="Master subject lines, body copy, and the art of avoiding spam filters." onClick={() => setSelectedModule('art')} />
                            <ModuleCard icon={<CtaIcon />} title="Double Your Clicks: Advanced Copywriting" description="Unlock the secrets to powerful personalization, A/B testing, and effective follow-ups." onClick={() => setSelectedModule('strategy')} />
                             <ModuleCard icon={<TeardownIcon />} title="Learn from the Best: Interactive Case Studies" description="Analyze real emails and test your knowledge before seeing the expert breakdown." onClick={() => setSelectedModule('teardowns')} />
                        </AcademyModuleSection>

                        <AcademyModuleSection
                            title="Module 3: Automate & Scale Your Growth"
                            subtitle="Put your email marketing on autopilot and use data to make smarter decisions that drive real results."
                        >
                           <ModuleCard icon={<AutomationIcon />} title="Put Growth on Autopilot: Master Automation" description="Build powerful welcome and nurture sequences that convert subscribers 24/7." onClick={() => setSelectedModule('automation')} />
                            <ModuleCard icon={<AnalyticsIcon />} title="Decode Your Data: Turn Analytics into Action" description="Learn which metrics actually matter and how to use them to fuel your growth." onClick={() => setSelectedModule('analytics')} />
                             <ModuleCard icon={<AutomationIcon />} title="Advanced Automation & Dynamic Content" description="Use behavioral triggers and dynamic content to create 1:1 email experiences at scale." onClick={() => setSelectedModule('advancedAutomation')} />
                        </AcademyModuleSection>
                        
                        <AcademyModuleSection
                            title="Module 4: Campaign Strategy Blueprints"
                            subtitle="Go from writing single emails to building effective, multi-step campaigns that achieve specific business goals."
                        >
                           <ModuleCard icon={<BlueprintIcon />} title="The Cold Outreach Blueprint" description="Learn how to structure a 5-email sequence for prospecting and personalization at scale." onClick={() => setSelectedModule('cold')} />
                            <ModuleCard icon={<BlueprintIcon />} title="The Newsletter Engagement Blueprint" description="Discover how to plan content, maintain consistency, and keep your audience engaged long-term." onClick={() => setSelectedModule('newsletter')} />
                             <ModuleCard icon={<BlueprintIcon />} title="The E-commerce Blueprint" description="Master the 'money-making' automations: abandoned carts, post-purchase, and win-back campaigns." onClick={() => setSelectedModule('ecommerce')} />
                             <ModuleCard icon={<BlueprintIcon />} title="The Re-engagement (Win-Back) Blueprint" description="Create campaigns to win back inactive subscribers and safely clean your list to protect deliverability." onClick={() => setSelectedModule('reEngagement')} />
                        </AcademyModuleSection>

                        <AcademyModuleSection
                            title="Module 5: Practical Application"
                            subtitle="Test your knowledge in real-world situations and see the impact of your decisions."
                        >
                            <ModuleCard icon={<BranchingIcon />} title="Scenario Simulator: Test Your Skills" description="Apply your knowledge in interactive, real-world email marketing challenges and get instant feedback on your decisions." onClick={() => setSelectedModule('simulator')} />
                        </AcademyModuleSection>
                    </div>
                );
        }
    };

    return (
        <div 
            className="fixed inset-0 bg-slate-900/80 backdrop-blur-sm z-50 flex items-center justify-center p-4 animate-fade-in"
            onClick={onClose}
        >
            <div 
                className="w-full max-w-5xl h-[90vh] max-h-[800px] bg-slate-900/95 dark-bg border border-white/10 rounded-2xl shadow-2xl flex flex-col"
                onClick={e => e.stopPropagation()}
            >
                <header className="flex items-center justify-between p-4 sm:p-6 border-b border-white/10 flex-shrink-0">
                    <div className="flex items-center gap-3">
                        <span className="text-purple-400"><AcademyIcon /></span>
                        <h2 className="text-xl sm:text-2xl font-bold text-white">Inbox Authority Academy</h2>
                    </div>
                    <button 
                        onClick={onClose} 
                        className="p-2 text-gray-400 rounded-full hover:bg-white/10 hover:text-white transition-colors"
                        aria-label="Close Academy"
                    >
                        <CloseIcon className="w-6 h-6" />
                    </button>
                </header>
                
                <div className="p-4 sm:p-6 border-b border-white/10 flex-shrink-0">
                    <div className="flex items-center gap-2 sm:gap-4 border border-white/10 p-1 rounded-lg bg-white/5 w-full sm:w-auto">
                        <TabButton 
                            label="Learning Modules" 
                            isActive={activeTab === 'modules'} 
                            onClick={() => setActiveTab('modules')} 
                        />
                        <TabButton 
                            label="AI-Powered Glossary" 
                            isActive={activeTab === 'glossary'} 
                            onClick={() => setActiveTab('glossary')} 
                        />
                    </div>
                </div>

                <main ref={mainContentRef} className="flex-grow p-4 sm:p-6 overflow-y-auto">
                    {activeTab === 'modules' ? renderModuleContent() : <Glossary />}
                </main>
            </div>
        </div>
    );
};

const TabButton: React.FC<{ label: string, isActive: boolean, onClick: () => void }> = ({ label, isActive, onClick }) => (
    <button
        onClick={onClick}
        className={`w-full text-center px-4 py-2 text-sm sm:text-base font-semibold rounded-md transition-colors ${isActive ? 'bg-purple-600 text-white shadow-md' : 'text-gray-300 hover:bg-white/10'}`}
    >
        {label}
    </button>
);
2. src/components/Academy/ModuleComponents.tsx
code
Tsx
import React, { useState } from 'react';
import { BackArrowIcon } from '../icons/CategoryIcons';

export const ModuleHeader: React.FC<{ onBack: () => void; title: string; subtitle: string; }> = ({ onBack, title, subtitle }) => (
    <div className="flex items-start gap-4">
        <button onClick={onBack} className="p-2 mt-1 sm:mt-0 text-gray-400 rounded-full hover:bg-white/10 hover:text-white transition-colors flex-shrink-0">
            <BackArrowIcon className="w-6 h-6" />
        </button>
        <div>
            <h3 className="text-2xl font-bold text-gray-100">{title}</h3>
            <p className="text-gray-400 text-sm mt-1">{subtitle}</p>
        </div>
    </div>
);

export const ModuleCard: React.FC<{ icon: React.ReactNode, title: string, description: string, onClick: () => void }> = ({ icon, title, description, onClick }) => (
    <button 
        onClick={onClick}
        className="bg-white/5 p-5 rounded-lg border border-white/10 text-left h-full flex flex-col group hover:bg-white/10 hover:border-purple-500/50 transition-all duration-300"
    >
        <div className="text-purple-400 mb-3">{icon}</div>
        <h4 className="font-bold text-lg text-white mb-2 group-hover:text-purple-300 transition-colors">{title}</h4>
        <p className="text-sm text-gray-400 flex-grow">{description}</p>
        <div className="mt-4 text-sm font-semibold text-purple-400 group-hover:text-purple-200 transition-colors">
            Start Lesson &rarr;
        </div>
    </button>
);

export const SectionWrapper: React.FC<{ title: string, subtitle: string, children: React.ReactNode }> = ({ title, subtitle, children }) => (
     <div className="bg-white/5 p-4 sm:p-6 rounded-lg border border-white/10">
        <h4 className="text-xl font-bold text-gray-100">{title}</h4>
        <p className="text-purple-400 font-semibold mb-3">{subtitle}</p>
        <div className="space-y-3 text-gray-300 text-sm leading-relaxed border-t border-white/10 pt-3">
            {children}
        </div>
    </div>
);

export const InfoBox: React.FC<{ icon: React.ReactNode, children: React.ReactNode }> = ({ icon, children }) => (
    <div className="p-4 bg-purple-900/30 border border-purple-500/50 rounded-lg flex flex-col sm:flex-row items-center gap-4 text-center sm:text-left">
        <div className="text-purple-300 flex-shrink-0">{icon}</div>
        <div>{children}</div>
    </div>
);

export const KnowledgeCheck: React.FC<{ question: string; options: { text: string; isCorrect: boolean }[]; explanation: string; }> = ({ question, options, explanation }) => {
    const [selected, setSelected] = useState<number | null>(null);
    const [answered, setAnswered] = useState(false);

    const handleSelect = (index: number) => {
        if (answered) return;
        setSelected(index);
        setAnswered(true);
    };

    return (
        <div className="bg-white/5 p-4 sm:p-6 rounded-lg border-2 border-dashed border-white/10">
            <h4 className="text-lg font-bold text-purple-300 mb-3">Check Your Understanding</h4>
            <p className="font-semibold text-gray-200 mb-4">{question}</p>
            <div className="space-y-2 mb-4">
                {options.map((option, index) => {
                    let buttonClass = 'bg-gray-900/50 border-gray-600 hover:bg-gray-800/60';
                    if (answered) {
                        if (option.isCorrect) buttonClass = 'bg-green-500/20 border-green-500 text-green-300';
                        else if (selected === index) buttonClass = 'bg-red-500/20 border-red-500 text-red-300';
                        else buttonClass = 'bg-gray-900/30 border-gray-700 text-gray-500';
                    }
                    return (
                        <button
                            key={index}
                            onClick={() => handleSelect(index)}
                            disabled={answered}
                            className={`w-full text-left p-3 text-sm rounded-lg border transition-all duration-300 flex items-center gap-3 ${buttonClass}`}
                        >
                            <span className="font-mono text-purple-400">[{index + 1}]</span>
                            <span>{option.text}</span>
                        </button>
                    );
                })}
            </div>
            {answered && (
                <div className="p-3 bg-gray-900/50 rounded-md text-sm text-gray-300 animate-fade-in">
                    <p className="font-bold text-gray-200">The Correct Answer:</p>
                    <p>{explanation}</p>
                </div>
            )}
        </div>
    );
};
3. src/components/Academy/DeliverabilityFoundations.tsx
code
Tsx
import React from 'react';
import { DnsIcon } from '../icons/CategoryIcons';
import { ModuleHeader, SectionWrapper, InfoBox, KnowledgeCheck } from './ModuleComponents';

export const DeliverabilityFoundations: React.FC<{ onBack: () => void }> = ({ onBack }) => (
    <div className="space-y-8 animate-fade-in">
        <ModuleHeader onBack={onBack} title="Domain Authentication" subtitle="Learn about the essential technical records (SPF, DKIM, DMARC) that prove your identity." />
        <SectionWrapper title="SPF: The Approved Senders List" subtitle="Sender Policy Framework">
            <p>Think of SPF as a public record that lists all the mail servers you've authorized to send email on behalf of your domain. It prevents scammers from sending emails that look like they came from you.</p>
        </SectionWrapper>
        <SectionWrapper title="DKIM: The Tamper-Proof Seal" subtitle="DomainKeys Identified Mail">
            <p>DKIM adds a unique, encrypted digital signature to every email you send. This signature verifies that the email's content has not been altered in transit.</p>
        </SectionWrapper>
        <SectionWrapper title="DMARC: The Enforcement Policy" subtitle="Domain-based Message Authentication">
            <p>DMARC builds on SPF and DKIM. It's a policy you publish that tells receiving mail servers what to do if an email fails the SPF or DKIM checks (e.g., send to spam, block).</p>
        </SectionWrapper>
        <InfoBox icon={<DnsIcon />}>
            <p className="text-purple-200 text-sm">Use our AI-powered DNS Generator in the main app to create these records instantly.</p>
        </InfoBox>
        <KnowledgeCheck
            question="Which authentication record acts like a tamper-proof seal?"
            options={[{ text: "SPF", isCorrect: false }, { text: "DKIM", isCorrect: true }, { text: "DMARC", isCorrect: false }]}
            explanation="Correct! DKIM adds a digital signature to ensure your email content hasn't been altered."
        />
    </div>
);
4. src/components/Academy/ArtOfTheInbox.tsx
code
Tsx
import React from 'react';
import { ModuleHeader, SectionWrapper, KnowledgeCheck } from './ModuleComponents';

export const ArtOfTheInbox: React.FC<{ onBack: () => void }> = ({ onBack }) => (
    <div className="space-y-8 animate-fade-in">
        <ModuleHeader onBack={onBack} title="Anatomy of a Great Email" subtitle="Master the core components of an email that gets opened and read." />
        <SectionWrapper title="Winning the Open" subtitle="Subject Lines & Preview Text">
            <p>Your subject line and preview text work together to capture attention. Clarity often beats cleverness.</p>
        </SectionWrapper>
        <SectionWrapper title="Driving the Action" subtitle="The Call to Action (CTA)">
            <p>Focus on the value the user gets, not just the action. "Get Your Free Guide" is better than "Download."</p>
        </SectionWrapper>
        <KnowledgeCheck
            question="When writing a Call to Action, what is the most effective approach?"
            options={[{ text: "Focus on the action", isCorrect: false }, { text: "Focus on the value/benefit", isCorrect: true }]}
            explanation="Exactly! People are motivated by what they'll gain."
        />
    </div>
);
(... For brevity in this specific response, I will summarize that you must create similar files for the remaining Academy modules listed in your original request, like AdvancedStrategy.tsx, RealWorldTeardowns.tsx, etc., using the same pattern of ModuleHeader, SectionWrapper, and KnowledgeCheck. You have the content for them in the previous dump.)